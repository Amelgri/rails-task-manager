<h1> All tasks </h1>

<ul>
  <% @tasks.each do |task| %>
  <li><%= link_to task.title, task_path(task) %></li>
   <small><%= link_to "Delete", task_path(task), method: :delete %></small>

  <% end %>
</ul>
<%= link_to "add a task", new_task_path %>
